function digPow(n, p){

let maxValue= 10;

 for(let powerX=1; powerX < maxValue; powerX++)
  {
   
      for(let powerY=1; powerY < maxValue; powerY++)
         {
	    
	          if(Math.pow(n, powerX) + Math.pow(p, powerY) === )
		     
		        }
			 }

			   // ...
			   }
